import { resolveCart } from './lib/fulfillment-resolver.mjs';
const SHOP_ID='28816619',ALLOWED_COUNTRIES=new Set(['CA','US']);function clean(v,n=120){return String(v||'').trim().slice(0,n)}
export default async(req)=>{if(req.method!=='POST')return Response.json({error:'Method not allowed'},{status:405,headers:{Allow:'POST'}});if(!process.env.PRINTIFY_API_TOKEN||!process.env.STRIPE_SECRET_KEY)return Response.json({error:'Shipping calculator is not configured.'},{status:500});let body;try{body=await req.json()}catch(_){return Response.json({error:'Invalid request.'},{status:400})}if(!Array.isArray(body.items)||!body.items.length||body.items.length>50||!/^cs_/.test(String(body.session_id||'')))return Response.json({error:'Invalid checkout request.'},{status:400});const mapped=resolveCart(body.items);if(!mapped.ok)return Response.json({error:'One or more cart options are not ready for shipping.'},{status:400});const a=body.address||{},country=clean(a.country,2).toUpperCase();if(!ALLOWED_COUNTRIES.has(country))return Response.json({error:'Shipping is currently available to Canada and the United States.'},{status:400});const full=clean(a.name,160),parts=full.split(/\s+/),address_to={first_name:parts.shift()||'Customer',last_name:parts.join(' ')||'-',email:clean(a.email,254)||'checkout@example.invalid',phone:clean(a.phone,40)||'Not provided',country,region:clean(a.region,80),address1:clean(a.address1,180),address2:clean(a.address2,180),city:clean(a.city,100),zip:clean(a.zip,30)};const missing=['region','address1','city','zip'].filter(k=>!address_to[k]);if(missing.length)return Response.json({error:'Please complete your shipping address.',missing},{status:400});const payload={line_items:mapped.items.map(x=>({product_id:x.product_id,variant_id:x.variant_id,quantity:x.quantity})),address_to};const r=await fetch('https://api.printify.com/v1/shops/'+SHOP_ID+'/orders/shipping.json',{method:'POST',headers:{Authorization:'Bearer '+process.env.PRINTIFY_API_TOKEN,'Content-Type':'application/json'},body:JSON.stringify(payload)});const d=await r.json().catch(()=>({}));if(!r.ok){console.error('Printify shipping quote failed',r.status,d);return Response.json({error:'Unable to calculate shipping for this address.'},{status:502})}const entries=Object.entries(d||{}).filter(([,v])=>Number.isFinite(Number(v))&&Number(v)>=0),preferred=entries.find(([k])=>k==='standard')||entries.find(([k])=>/standard/i.test(k))||entries[0];if(!preferred)return Response.json({error:'No shipping method is available for this order.'},{status:422});const [shippingName,rawAmount]=preferred,shippingAmount=Math.round(Number(rawAmount)),p=new URLSearchParams();p.set('collected_information[shipping_details][name]',address_to.first_name+' '+address_to.last_name);p.set('collected_information[shipping_details][address][country]',country);p.set('collected_information[shipping_details][address][line1]',address_to.address1);if(address_to.address2)p.set('collected_information[shipping_details][address][line2]',address_to.address2);p.set('collected_information[shipping_details][address][city]',address_to.city);p.set('collected_information[shipping_details][address][state]',address_to.region);p.set('collected_information[shipping_details][address][postal_code]',address_to.zip);p.set('shipping_options[0][shipping_rate_data][type]','fixed_amount');p.set('shipping_options[0][shipping_rate_data][fixed_amount][amount]',String(shippingAmount));p.set('shipping_options[0][shipping_rate_data][fixed_amount][currency]','usd');p.set('shipping_options[0][shipping_rate_data][display_name]',shippingName);p.set('shipping_options[0][shipping_rate_data][tax_behavior]','exclusive');p.set('shipping_options[0][shipping_rate_data][tax_code]','txcd_92010001');p.set('metadata[quoted_shipping_cents]',String(shippingAmount));p.set('metadata[quoted_shipping_method]',shippingName);const sr=await fetch('https://api.stripe.com/v1/checkout/sessions/'+encodeURIComponent(body.session_id),{method:'POST',headers:{Authorization:'Bearer '+process.env.STRIPE_SECRET_KEY,'Content-Type':'application/x-www-form-urlencoded'},body:p.toString()});const sd=await sr.json().catch(()=>({}));if(!sr.ok){console.error('Stripe shipping update failed',sr.status,sd?.error?.message||sd);return Response.json({error:'Unable to update checkout shipping.'},{status:502})}return Response.json({ok:true,method:shippingName,amount:shippingAmount,currency:'usd'})};